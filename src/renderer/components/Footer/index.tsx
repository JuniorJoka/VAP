import * as React from "react";

import { Controls } from "../Controls";
import { Wrapper } from "./FooterStyle";

export const Footer = () => {
  return (
    <Wrapper>
      <Controls />
    </Wrapper>
  );
};
