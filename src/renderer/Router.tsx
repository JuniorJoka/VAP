import * as React from "react";
import { Switch } from "react-router-dom";
import { Vap } from "./App";

export const Router: React.FC = () => {
  return (
    <Vap>
      <Switch></Switch>
    </Vap>
  );
};
