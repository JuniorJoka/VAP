import Controls from './Controls';

export default {
  title: 'Component/Conrols',
  component: Controls,
  parameters: {
    layout: 'fullscreen',
  },
};

const Template = (args) => <Controls />;

export const Default = Template.bind({});
